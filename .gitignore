# Dependencies
node_modules/
*/node_modules/

# Build output
dist/
*/dist/
*.tsbuildinfo

# Runtime data — never commit secrets or the live DB
.env
.env.local
data/
stacks/
*.sqlite
*.sqlite-*
*.db

# Live deployment config (nutze docker-compose.example.yml als Vorlage)
/docker-compose.yml

# TLS client certs / keys for remote Docker endpoints
certs/
*.pem
*.key
*.crt

# Logs
*.log
logs/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
NAMING-UND-LOGO.md
/docker-compose.override.yml
Logos/README.txt

# Docker Hub repo overview (paste manually; not for git/image)
DOCKERHUB.md

# Native Android-App + Design-Spec: privat, NICHT auf GitHub
clients/
docs/superpowers/

# ── Closed-Source: Quellcode + Build-Tooling bleiben LOKAL, nie auf GitHub ──
/server/
/web/
/shared/
/scripts/
/.github/
/Dockerfile
/package.json
/package-lock.json
/tsconfig.base.json
/eslint.config.js
/.prettierrc.json
/.npmrc
